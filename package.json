{
  "name": "market-scope",
  "private": true,
  "description": "Monorepo root: shared tooling and CI orchestration. Application code lives in api/ and app/.",
  "workspaces": [
    "api",
    "app"
  ],
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "ci": "npm run format:check && npm run typecheck"
  },
  "devDependencies": {
    "prettier": "^3.4.2"
  }
}
