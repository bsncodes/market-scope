name: Setup
description: Set up Node and install workspace dependencies from the root lockfile.

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: 22
        cache: npm

    - run: npm ci
      shell: bash
